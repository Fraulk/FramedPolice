## 2025-12-01

Added checks to make sure the roles exist before trying to assign them and a tiny pause (0.5 seconds) between removing the Welcome role and adding Padawan, so Discord has time to process both changes properly instead of skipping one. Also added some logs to maybe understand better what's happening
